interface:
  display_name: "AutoAgent Workflow Authoring"
  short_description: "Create and validate AutoAgent Workflows"
  default_prompt: "Create or modify an AutoAgent Workflow using only the stable public authoring API, then validate and test it."
